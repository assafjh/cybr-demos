# onprem-base.yml
# Load this policy into the 'root' branch:
# conjur policy load -b root -f onprem-base.yml

- !group
  id: admins

- !user
  id: admin01
  owner: !group admins

- !permit
  role: !group admins
  privileges: [ read, update, create ]
  resource: !policy root

- !grant
  role: !group admins
  member: !user admin01

# Define data branch and delegate to Ansible
- !policy
  id: data
  owner: !group admins
  body:
    - !policy
      id: ansible
      owner: !group /admins
      body:
        - !group admins

        - !permit
          role: !group admins
          privileges: [ read, update, create ]
          resource: !policy ansible

        - &ansible-admin-users
          - !user ansible-admin01

        - !grant
          role: !group admins
          members: *ansible-admin-users
